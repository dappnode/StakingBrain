{
  "name": "@stakingbrain/brain",
  "type": "module",
  "version": "0.1.0",
  "license": "GPL-3.0",
  "scripts": {
    "start:dev": "ln -s ../../ui/build/ ./dist && node dist/index.js",
    "dev": "tsc -w",
    "build": "yarn run clean:dist && tsc -p tsconfig.json",
    "clean": "yarn run clean:modules && yarn run clean:dist",
    "clean:modules": "rimraf node_modules",
    "clean:dist": "rimraf dist"
  },
  "dependencies": {
    "@stakingbrain/common": "^0.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "nodemon": "^2.0.20",
    "rimraf": "^4.1.1",
    "typescript": "^4.9.4"
  }
}
